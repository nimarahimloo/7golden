FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 5173

# Proxy target for API (docker network name of backend service)
ENV VITE_PROXY_TARGET=http://backend:3001

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
