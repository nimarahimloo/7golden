# Local development defaults for the Base44 dev environment.
# Real values supplied from the Base44 dashboard are delivered to the
# container via /run/base44/app.env and always override these (it is listed
# last in docker-compose.base44.yml's env_file list).
VITE_BASE44_APP_ID=6a9ea5d67a95141fb1f84b4a
VITE_BASE44_APP_BASE_URL=https://base44.app
